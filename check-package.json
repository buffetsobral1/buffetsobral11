{
  "name": "buffet-sobral-deployment-check",
  "version": "1.0.0",
  "description": "Script para verificar se o projeto está pronto para deployment",
  "main": "check.js",
  "scripts": {
    "check": "node check.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "node-fetch": "^3.3.2"
  }
}